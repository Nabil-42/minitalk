# Installation — minitalk

## Requirements

- OS: Linux or macOS
- gcc
- Make

## Build

```bash
git clone https://github.com/Nabil-42/minitalk.git
cd minitalk
make
```

Two binaries are produced: `server` and `client`.